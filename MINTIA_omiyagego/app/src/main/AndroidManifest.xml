<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tangcan0823.mintia_omiyagego">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.tangcan0823.mintia_omiyagego.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/DrawerLayoutTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".DetailActivityFood1"/>
        <activity android:name=".DetailActivityFood2"/>
        <activity android:name=".DetailActivityFood3"/>

    </application>

</manifest>